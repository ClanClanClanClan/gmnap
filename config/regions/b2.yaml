# R58 surname_exact supplement — EXACT-only direct-match entries for the
# manager's surname tier (score 10 -> confidence 0.95). Derived from the
# R58 adjudicated pilot ground truth (data/eval/pilot_adjudicated.json;
# both-agree, leaf-confident verdicts only) under the curation policy:
# no cross-region bearers, no diaspora-split surnames, cleaned token
# length >= 4 (>=2 for diacritic-bearing), ASCII variant listed only when
# still region-specific. Collisions with any hardcoded set or another
# region's supplement are DROPPED at load time (see _init_surname_patterns).
surname_exact:
  - kršek        # adjudicated bearer
  - krsek
  - kurpisz      # adjudicated bearer
  - mijatović    # adjudicated bearer + Nenad Mijatovic
  - mijatovic
  - cvitanić     # adjudicated bearer
  - cvitanic
  - raonić       # adjudicated bearer
  - raonic
  - aleksić      # adjudicated bearer
  - aleksic
  - džamić       # adjudicated bearer
  - dzamic
  - milošević    # adjudicated bearer
  - milosevic
  - obradović    # adjudicated bearer
  - obradovic
  - grzywny      # adjudicated bearer
  - sztonyk      # adjudicated bearer
  - lucić        # adjudicated bearer (diacritic form only; ascii 'lucic' cross-Slavic)
  - bajović      # adjudicated bearer
  - bajovic
  - petković     # adjudicated bearer
  - petkovic
  - đogaš        # adjudicated bearer Đogaš
  - gotovac      # same bearer
  - mandarić     # adjudicated bearer
  - mandaric
  - brzeźniak    # adjudicated bearer
  - brzezniak
  - czyż         # adjudicated bearer
  - czyz
  - šebek        # Stjepan Šebek
  - sebek
  # R58.7: Romanian entries moved from b3.yaml (B3 is 'Greek World'; the
  # codebase maps RO -> B2 'South-Slavic & Central Europe')
  - castravet    # adjudicated bearer
  - robu         # adjudicated bearer
  - marinescu    # adjudicated bearer
  # R59.4: Polish -sson bearer (protect from the Nordic A3 -sson signature)
  - mathisson    # adjudicated bearer

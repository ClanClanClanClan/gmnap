# GMNAP V7 Docker Requirements - MINIMAL MODE
# Minimal set for HTTP server (Phase 1: Rules only, no ML)
# For ML support, build locally with pre-compiled fasttext binary

# Web Server (essential for gmnap.cli serve)
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
httpx==0.27.2

# Required by RegionManager imports
ruamel.yaml==0.18.6
networkx==3.3
jsonschema==4.21.1

# Async utilities
aiohttp==3.9.5

# Monitoring
prometheus-client==0.20.0

# Utilities
orjson==3.10.7
psutil==6.0.0
click==8.1.7

# NOTE: ML packages (fasttext, xgboost, scikit-learn, numpy) excluded
# Reason: fasttext-wheel fails to compile in Docker (OOM during g++ compilation)
# Alternative: Use rules-based detection (Phase 1) which works without ML
# For full ML support: Build locally or use pre-built fasttext binary
